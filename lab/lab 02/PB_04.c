// 04
#include <stdio.h>
#include <stdlib.h>

int main() {
    char input_str[10];

    fgets(input_str, 10, stdin);
    float num = atof(input_str);

    if (num <= 0){
        printf("Error: Salary must be greater or equal to 0");
    } else {
        if 
    }
    
    return 0;
}
